name: "Custom herd immunity"
description: "Custom automation for herd immunity"
type: "triage_rule"
triage_abuse_reports: true
default_actions: ["quarantine_message"]
source: |
  type.inbound
  and triage.user_reports.count > 5